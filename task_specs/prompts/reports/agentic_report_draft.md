你现在是主要作者。请基于证据包、侦察笔记和大纲，写一份中文 Markdown 报告初稿。

写作风格目标：{style}

证据包：

```json
{evidence_pack}
```

侦察笔记：

```markdown
{scout_notes}
```

大纲：

```markdown
{outline}
```

写作要求：

- 报告必须有一个有吸引力的标题，标题应表达发现，而不是只说“分析报告”。
- 开头用 2-4 段给出“这批语料显示了什么变化”，不要先罗列数字。
- 主体围绕 3-5 条洞察展开，每条洞察都要包含：
  - 研究判断；
  - 证据链；
  - 一个具体例子；
  - 为什么这对计算社会科学方法演化重要；
  - 保留意见。
- 必须自然引用 evidence anchor，例如 edge id、trajectory id 或 pattern id。
- 可以使用少量数字，但不要做纯数字汇报，不要大面积表格。
- 必须区分“证据支持的判断”和“下一步需要审计的猜想”。
- 不要编造证据包之外的论文、年份、节点、引文或因果机制。
- 不要使用 emoji 或装饰性符号。
- 避免把“证据提示”写成“已经证明”。除非证据包明确支持采纳、扩散或因果机制，否则使用“提示”“可能”“需要审计”。

直接输出中文 Markdown 初稿。
