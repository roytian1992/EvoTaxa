你现在是报告架构师。请根据证据包和侦察笔记，设计一份吸引人的中文 Markdown 报告大纲。

写作风格目标：{style}

证据包：

```json
{evidence_pack}
```

侦察笔记：

```markdown
{scout_notes}
```

要求：

- 报告要像研究备忘录或短论文，而不是 dashboard 导出。
- 标题要有判断和张力，不要叫“关系类型分布”这种表头。
- 每一节都要说明：核心论点、证据锚点、必要的保留意见。
- 数字只能作为支撑，不要让数字成为叙事主角。
- 必须保留一个“我们还不能声称什么”的部分。

只输出大纲，不要写完整报告。
