你现在是最终作者。请根据审稿意见，重写并定稿这份中文 Markdown 报告。

写作风格目标：{style}

证据包：

```json
{evidence_pack}
```

初稿：

```markdown
{draft}
```

审稿意见：

```markdown
{critique}
```

定稿要求：

- 写成一份真正能给研究合作者看的 insight report，而不是流水账。
- 开头要有清晰的“中心发现”。
- 主体用少数有力量的洞察组织，不要机械覆盖所有模式。
- 每个强判断都要锚定至少一个 pattern id、edge id、trajectory id、节点对或 quote。
- 把数字藏在论证里，只在必要时出现。
- 对弱证据、抽取伪影、泛机器学习迁移和社会科学相关性不足的问题要诚实。
- 结尾给出下一轮研究动作：该审计哪些边、该重跑哪些抽取、该怎么让图和报告更可信。
- 不要使用 emoji 或装饰性符号。
- 不要把“证据提示”写成“已经证明”。涉及采纳、扩散、制度化效果、因果机制、方法论安全性时，除非证据包有直接证据，否则必须降调。
- 不要输出审稿说明，不要解释你如何修改，直接输出最终中文 Markdown。
